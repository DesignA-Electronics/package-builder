NAME=openssl
VERSION=1.0.0g
SOURCE=http://www.openssl.org/source/openssl-${VERSION}.tar.gz

download_unpack "${SOURCE}"

CC="${CROSS}gcc ${CFLAGS}" ./Configure --prefix=$1 linux-armv4
make install

fix_install_paths "$1"
build_generic_package "$1"

