NAME=libunwind
VERSION=1.0.1
SOURCE=http://download.savannah.gnu.org/releases/libunwind/libunwind-${VERSION}.tar.gz

post_unpack() {
    patch -p1 < ${SUPPORT_FILES}/patches/libunwind-ucontext.patch
}

do_generic "$1"

