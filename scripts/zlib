NAME=zlib
VERSION=1.2.5
SOURCE=http://zlib.net/zlib-${VERSION}.tar.bz2
INSTALL_PARAMS=prefix=/

download_unpack "${SOURCE}"

CC=${CROSS}gcc CFLAGS="-march=armv4t -mtune=arm9tdmi" ./configure
do_make
do_install "$1"

build_generic_package "$1"
